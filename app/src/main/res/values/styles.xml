<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/PC_Blue</item>
        <item name="colorPrimaryDark">@color/PC_Blue</item>
        <item name="colorAccent">@color/PC_Blue</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/PC_Blue</item>
        <item name="actionMenuTextColor">@color/PC_Blue</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- just change the parent -->
    <style name="AppTheme.Dark" parent="MaterialDrawerTheme">
        <item name="colorPrimary">@color/PC_Blue</item>
        <item name="colorAccent">@color/PC_Blue</item>
        <item name="colorPrimaryDark">@color/PC_Blue</item>

    </style>
</resources>
